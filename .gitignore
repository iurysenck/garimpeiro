# Segredos e dados pessoais — NUNCA commitar
# (comentários precisam ficar em linha própria — git não aceita comentário inline)
.env
.env.*
perfil.md
# gerado pelo wizard; tem suas preferências (use config.example.yaml como base)
config.yaml

# Sessão logada do Chrome (nodriver) — contém seus logins
.nddata/
.botprofile/
.chromeprofile/

# Banco, logs, estado
vagas.db
garimpo.log
errors.log
source_health.json

# Painel gerado / build
public/index.html
public/version.json
_dist/

# Ambiente Python
.venv/
__pycache__/
*.pyc
